# 除外事項（非機能要件）

- 認証/認可: APIキーやOAuthなどの認証機構は実装しない。
- CORS / レート制限: 設定しない（公開アクセスを前提）。
- テスト自動化: CI による自動テストは実施しない（手動テストのみ）。
- ログ収集・アラート: Application Insights 等のログ収集やアラートは導入しない。
- 高可用性・冗長化: Consumption Plan を利用するが、AZ単位の冗長化や専用SLA 構成は行わない。
- コスト上限: 特に設定しない（予算上限は設けない）。
